        /* ── Footer nhỏ (LBTV-208: badge 08.png + số trang + tên HOA) ── */
        .page-footer {
            position: absolute;
            left: 0;
            width: 210mm;
            bottom: 8mm;
            text-align: center;
        }

        .footer-banner-wrap {
            position: relative;
            display: inline-block;
        }

        .footer-banner {
            display: block;
            width: 33mm;
            height: auto;
            margin: 0 auto;
        }

        .footer-page-number {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: #FFFFFF;
            font-weight: bold;
            font-size: 17px;
            letter-spacing: 0.2mm;
            white-space: nowrap;
        }

        .footer-name {
            margin-top: 1.4mm;
            color: #0A0A0A;
            font-size: 16px;
            font-weight: bold;
            letter-spacing: 0.15mm;
            text-transform: uppercase;
            text-align: center;
        }
