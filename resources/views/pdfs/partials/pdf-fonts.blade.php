/* Font + size mặc định — KHÔNG set text-align toàn trang (tránh đụng TOC, bảng, pill). */
body,
.page {
    font-family: 'svn-poppins', sans-serif;
    font-style: normal;
    letter-spacing: 0;
}

body {
    font-weight: normal;
    font-size: 14px;
    line-height: 140%;
}
